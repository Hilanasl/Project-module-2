<section class='container'>
        {{#each catCards}}
        <div class='card'>
        <img src="{{this.image}}"/>
        <p>{{this.title}}</p>
        <a href='/arrond/{{this.category}}/{{this._id}}' class='info'>See more info</a>
        </div>
        {{/each}}
</section>